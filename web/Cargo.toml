[package]
name = "beta-credible-web"
version = "0.1.0"
edition = "2021"

[dependencies]
beta-credible-stats = { path = "../stats" }
yew = { version = "0.21.0", features = ["csr"] }
web-sys = { version = "0.3", features = ["HtmlInputElement"] }
getrandom = { version = "0.2", features = ["js"] }
